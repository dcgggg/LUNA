# LUNA 开发规则

用户约定整理日期：2026-09-12。本文规定开发行为与验收目标，不代表所列功能已经实现。

## 1. 项目与当前阶段

- 正式名称：LUNA — Local field potential Unified Network Analysis platform。
- 当前主项目为 Python 实现。本地开发目录：`C:\Users\PC\Documents\ChatGPT\LID_Tetrode_Analysis`。用户上传 GitHub 的代码均来自此目录；远端可能落后于本地未提交修改。
- 先核实当前工作区、Git 根目录、分支和 remote；不凭历史名称判断仓库。不得擅自重命名本地目录、创建替代项目或修改其他历史版本。
- 当前专注单个记录 session：完善原始数据预览、伪影处理、PSD、FOOOF/谱参数化、频带功率、功能连接六个模块的计算逻辑、GUI、保存与重载。
- 后续才扩展为“项目 → 被试 → 访视 → 记录 session → 通道”，用用药、刺激、行为等状态标签筛选。该结构仍可调整；未经新任务要求，不提前实施多层级管理或复杂插件框架。
- 可以清晰分离数据、计算、绘图、配置与任务管理，保留必要扩展接口；新算法在用户提出需求后加入，不自行扩大功能范围。
- 优先级：计算正确性 → 稳定性与保存可靠性 → GUI 体验 → 性能优化 → 新算法。

## 2. 自主权限与 Git

- 在当前任务范围内，普通 Bug 修复、跨文件重构、分析算法和默认参数调整均可自行完成，无需逐项询问；必须记录依据、影响和验证。
- 为改善使用体验，可以自行调整面板位置和交互流程，保持必要功能可达并验证受影响流程。
- 可以在项目虚拟环境内新增、升级、移除依赖；记录原因、版本和验证，不修改系统 Python，不无故整体升级依赖。
- **git commit 和 git push 必须在执行前获得用户明确授权。commit 授权不自动包含 push；既往其他任务的授权不视为本次授权。** 询问前完成代码、日志、检查及可审阅的变更摘要。未获授权时保留本地修改。
- 不覆盖用户已有修改；不执行破坏性 Git 重置、强推或无关批量删除。新请求若仅要求审查/建议，则只审查，不将此处自主权限解释为修改授权。
- 用户允许对相互独立的任务使用子代理。明确各代理的文件职责，避免同时修改同一文件；主代理负责整合、冲突处理和最终验证。无需为简单任务强行并行。

## 3. 开始工作与事实依据

- 先阅读现有 README、适用目录规则、依赖配置、启动脚本及相关测试，追踪实际调用关系再修改。
- 以当前代码和运行证据判断状态；历史聊天、提示词和规划不是完成证据。区分已实现、已验证、待验证、未实现。
- 查找代码优先使用快速文件/文本搜索；按调用关系读取必要内容，不只根据文件名推断。
- 保留合理现有结构，针对根因做适度修改，不为减少行数合并科学含义不同的实现，不以吞掉异常或静默回退掩盖错误。
- 本文编写时未核实当前 GUI 框架、Python 版本、启动命令和测试命令。首次本地接手时，从实际文件核实并在 `docs/DEVELOPMENT.md` 记录相对入口、环境准备、GUI 启动和验证命令；成功运行后才标为已验证。找不到入口时报告，不猜造命令或替代入口。
- 不因本轮修改而顺带实现本文中全部尚未完成的目标；按用户当前任务和阶段推进。

## 4. 科学计算与算法来源

- 优先使用成熟工具包的官方实现；现有实现不能满足需求时才自行编写，并说明不适用原因。
- 核对安装版本的官方文档与接口，记录方法来源、工具包版本、参数含义、单位、范围、默认值、自动值含义及验证依据。
- 不静默用另一种算法替代调用失败的官方实现。明确区分依赖缺失、参数不合法、数据不满足条件及计算失败。
- 算法/默认值可调整，但需记录前后差异、科学依据、结果影响、旧缓存失效与是否需要重算。重构或布局修改本身应保持科学计算语义。
- “好的结果”指正确、可解释、可复现；不能为了平滑、美观、显著性或预期方向修改数据或算法。
- 明确数组维度、通道索引、信号单位、频率网格、预处理、频带边界、归一化、聚合顺序及有向连接方向。无效值不当作真实零。
- 明确区分原始谱、非周期背景、去背景观测谱与周期拟合谱；显示平滑、插值、遮罩不得冒充原始计算结果。
- 优先复用已有数值验证。涉及新算法或计算语义变化时，按风险补充官方参考/已知结果的合成信号验证；真实数据能运行不等于算法正确。

## 5. 参数、数据与缓存

- 方法选择在前，通用参数置顶，专属参数按方法分组；常用参数直接显示，高级参数折叠。
- 提供中文参数说明，包含含义、单位、有效范围及自动值含义；英文界面提供对应英文说明。
- 一般模块中，当前适用参数显示清晰启用背景；不适用参数保留可见、灰化且真正禁用鼠标/键盘/滚轮编辑，并说明原因。PSD 是明确例外：Welch 与 Multitaper 的专属参数按当前方法完全隐藏另一组及其标题，不保留空白；当前方法内部因开关依赖而暂不可用的参数仍可灰化说明。未勾选的可用复选框不等同于禁用。
- 切换方法保留各方法输入值。初始化、载入配置、恢复默认和程序更新均走同一状态逻辑；同时遵守运行期间已有控件锁定规则。
- 只校验和传递当前实际生效参数。GUI 值、保存配置、计算参数和元数据一致。
- 采样率和通道信息优先从文件读取。信号单位、通道—脑区映射核实后使用，不写死历史实验映射；测试配置与通用默认配置分离。
- 数据、映射、预处理或有效参数变化时，按依赖使相关结果/缓存失效；不能错误复用其他 session 或配置的结果。单纯显示设置变化不无故重算。
- 受影响模块显示“参数已变更，需重新计算”，取消当前有效结果的完成高亮；历史结果保留并显示其原参数。
- 后台结果必须与启动时的数据、参数快照和任务标识对应，避免切换页面/数据后写回错误位置。

## 6. GUI 与语言

- 支持中文/英文切换；控件、状态、错误提示、帮助随语言切换，算法名保留标准名称。图表语言可独立选择，用于英文科研图导出。
- 用户定义的脑区/通道名称及数据标识不自动翻译；切换语言不修改数据、参数、缓存标识或计算结果。
- 同脑区通道使用同色系且可区分，不同脑区主色尽量明显；分类配色在页面、筛选和重排间稳定，与数值热图色标分开管理。
- 文字、单位、标题、刻度、图例完整可读；必要时合理换行、折叠或滚动，不依靠过小字体解决布局。
- 每张连接矩阵有独立 colorbar 和预留空间；同指标可比图保持合理一致的数值映射。重绘正确释放旧 axes、colorbar 和事件绑定。
- 耗时任务保持 GUI 响应并显示状态；计算完成要高亮。失败、取消、结果过期与保存失败有明确状态，不误报成功。
- 验证 GUI 内显示和导出，覆盖窗口化/最大化及实际可用的 DPI 条件；未实际检查的条件明确标记。

## 7. 保存、重载与兼容

- 首次让用户选择结果根目录并记住；按 session 和每次分析分别保存，使用稳定标识避免同名覆盖。
- 计算成功后自动保存结果、实际有效参数和必要元数据；图片按需导出。界面独立显示“计算完成”与“保存成功/失败”，失败时保留可恢复的内存结果并提供重试。
- 保存数据来源/标识、通道与映射、频率/时间轴、单位、预处理、算法和依赖版本、时间、格式版本等重载所需信息；按实际结果类型保存必要内容。
- 使用可靠写入方式避免中断产生被误认为完整的结果。保存失败不能标为已保存，不覆盖旧成功结果。
- 原始文件不可用时，仍能重载已保存分析结果并重绘；依赖原始信号的操作再提示定位文件。不要求复制全部原始数据才能查看结果。
- 尽量兼容历史配置/结果，显式保存格式版本；无法兼容时说明或迁移，保留原文件，不静默误读。

## 8. 每次修改后的真实数据验证

- 固定只读测试文件：`C:\Users\PC\Documents\ChatGPT\testdata\LID-T80_all_channels-epo.fif`。
- 每次代码修改后均使用该文件验证受影响功能及必要上下游；公共读取、预处理、缓存或跨模块重构应回归全部六个模块。纯文档修改可验证文档/路径准确性，无需伪装为数值回归。
- 先核实文件存在、可读取、采样率、通道信息及单位；脑区映射经核实形成测试配置。不得直接套用 M1/STR/PF/SNr 历史映射，不编造数据属性。
- 不覆盖测试源文件；测试输出使用独立本地目录。用户提供的聊天附件临时路径不能替代正式 Windows 路径写入项目运行逻辑。
- GUI 修改必须实际加载这份数据检查受影响交互；计算、保存和重载相关修改应检查对应完整流程，语法检查不能替代。
- 若依赖、文件或 GUI 环境不可用，继续可完成的静态/合成验证，记录具体阻碍并明确“真实数据验收未完成”，不能假称通过。
- 记录基线、运行命令、数据规模、有效参数、测试项目和结果。算法不变的重构用合理数值容差比较；算法变化说明差异。性能提升需测量，不编造比例。

## 9. 日志、进度与数据保护

- 每次修改追加 `docs/DEV_LOG.md`，记录时间（含时区）、任务、涉及文件、原因、关键差异、验证及未解决问题；算法/默认参数修改额外记录来源、版本、前后值、结果影响和重算要求。
- 日志只保留必要验证摘要，不写原始实验数据、患者身份信息或真实数值序列。真实数据衍生数值、完整报告和截图可本地保存，但不得随 Git 提交。
- 原始数据、衍生结果、含真实数据的测试截图、缓存和本地环境不加入 Git。提交前检查忽略规则及待提交内容；已被跟踪的文件不能只靠新增 ignore 规则处理，应明确列出并妥善处理。
- 按需维护 `docs/PROJECT_STATUS.md`：完成状态、验证证据、待解决问题；`docs/ROADMAP.md`：后续规划。缺失时在需要记录时建立，不能把计划写成实现。
- 完成后用中文简述修改、理由、验证、限制及日志位置。代码变更完成但真实数据未验收时明确区分状态。

## 10. 本地首次核实清单

- [ ] 当前工作区与 Git 根目录、remote、分支、未提交变更。
- [ ] Python 环境、依赖版本、实际 GUI 框架、启动入口与命令。
- [ ] 六个模块当前实现/运行/保存/重载状态，已有测试入口。
- [ ] 固定 FIF 文件可用性，读取的元数据、单位和已确认测试映射。
- [ ] 本地测试输出、结果根目录配置、Git 忽略规则。

上述清单当前未由本文作者进行本地验证。由本地代理根据事实更新开发文档与状态，不得自行标记全部完成。
